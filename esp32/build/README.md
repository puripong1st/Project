# esp32/build/ — Compiled Firmware Output Directory
#
# ไฟล์ในโฟลเดอร์นี้ถูกสร้างโดย arduino-cli อัตโนมัติ
# ห้ามแก้ไขด้วยตนเอง
#
# วิธีสร้างไฟล์ binary:
#   1. เปิด VS Code ใน Project root
#   2. กด Ctrl+Shift+P → "Tasks: Run Task" → "Build ESP32 Firmware"
#   หรือรันคำสั่ง:
#   arduino-cli compile --fqbn esp32:esp32:esp32 --output-dir ./build ../esp32.ino
